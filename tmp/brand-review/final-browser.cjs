const {chromium}=require('C:/Users/Amirsaly/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{
 const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});
 const page=await browser.newPage({viewport:{width:360,height:900}});
 const errors=[];page.on('pageerror',e=>errors.push(e.message));
 await page.goto('http://127.0.0.1:5173/');
 await page.evaluate(()=>document.fonts.ready);
 assert.equal(await page.evaluate(()=>document.fonts.check('16px Inter')),true);
 const logo=page.locator('header img');assert.equal(await logo.getAttribute('src'),'/salyco-logo-navy.svg');
 await page.getByRole('button',{name:'باز کردن منو'}).click();
 assert.equal(await page.locator('#mobile-menu').evaluate(e=>e.inert),false);
 await page.keyboard.press('Escape');
 assert.equal(await page.locator('#mobile-menu').evaluate(e=>e.inert),true);
 await page.goto('http://127.0.0.1:5173/products');
 const card=page.locator('article').first();await card.waitFor();
 assert.equal(await card.locator('img').evaluate(e=>getComputedStyle(e).objectFit),'contain');
 assert.equal(await card.getByRole('button').count(),0);
 await card.click();
 await page.getByRole('heading',{level:1}).waitFor();
 await page.waitForTimeout(300);
 assert.equal(await page.getByText('۱۴۰ کیلوگرم',{exact:true}).count()>0,true);
 await page.screenshot({path:'tmp/brand-review/product-mobile.png',fullPage:true});
 await page.goto('http://127.0.0.1:5173/contact');
 assert.equal(await page.getByLabel('نام و نام خانوادگی').count(),1);
 assert.equal(await page.locator('a[href="tel:09933921096"]').count()>0,true);
 await page.evaluate(()=>document.documentElement.style.fontSize='200%');
 assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);
 await page.screenshot({path:'tmp/brand-review/contact-zoom.png',fullPage:true});
 await page.evaluate(()=>document.documentElement.style.fontSize='');
 await page.goto('http://127.0.0.1:5173/');await page.waitForTimeout(300);
 await page.screenshot({path:'tmp/brand-review/home-mobile-final.png'});
 assert.deepEqual(errors,[]);
 console.log('PASS: local product content, contained images, detail navigation, mobile menu/Escape, labels, contact links, Inter, 200% text and JS errors.');
 await browser.close();
})().catch(e=>{console.error(e);process.exit(1)});
